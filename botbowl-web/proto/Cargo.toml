[package]
name = "botbowl-web-proto"
version = "0.1.0"
edition = "2021"
description = "Wire types shared by the botbowl web server and the wasm client. No engine dependency: compiles on host and on wasm32."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
